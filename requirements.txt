# ── Core API ──────────────────────────────────────────────
fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2.6

# ── Database ──────────────────────────────────────────────
motor>=3.3          # async MongoDB
pymongo>=4.6

# ── Data / ML ─────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3

# ── Optional but recommended ──────────────────────────────
xgboost>=2.0        # improves model accuracy; fallback to sklearn GBM if missing
shap>=0.44          # XAI feature importance; fallback to built-in if missing

# ── Utilities ─────────────────────────────────────────────
matplotlib>=3.7
scipy>=1.11
python-multipart>=0.0.9
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
httpx>=0.26
aiofiles>=23.2
python-dotenv>=1.0
